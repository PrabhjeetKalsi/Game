export const gameWidth = window.innerWidth * 0.75;
export const gameHeight = window.innerHeight * 0.6;
export const modAbs = (value, modulo) => ((value % modulo) + modulo) % modulo;
export const MOVE_DIST = 40;
